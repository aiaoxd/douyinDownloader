# douyinDownloader 运行依赖（Python 3.9+）
#
# 装这些就能跑 —— 默认走「协议模式」（纯 HTTP，不启动浏览器）。
# 另需本机安装 ffmpeg 与 ffprobe（图文 / 实况图合成用，命令行能跑 ffmpeg -version 即可）。
# 想要浏览器回退能力，再另装 requirements-browser.txt。

# 网络请求
requests>=2.28.0

# 下载进度条
tqdm>=4.65.0

# 图文作品的图片规格化（抖音图文下下来是 webp，需转成 JPEG 再合成）
Pillow>=9.0.0

# 视频/音频封装与时长解析（底层仍调用系统 ffmpeg）
ffmpeg-python>=0.2.0
