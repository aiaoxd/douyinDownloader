# douyinDownloader 运行依赖（Python 3.9+）

# 网络请求
requests>=2.28.0

# 下载进度条
tqdm>=4.65.0

# 图文作品的图片规格化（统一转 JPEG 并 resize）
Pillow>=9.0.0

# 视频/音频合并（同时需要本机装好 ffmpeg 与 ffprobe）
ffmpeg-python>=0.2.0

# 浏览器监听方案：监听 aweme/detail 接口获取真实播放地址
# pip 安装后还需执行一次：playwright install chromium
playwright>=1.40.0
